[phases.setup]
nixPkgs = ['nodejs-18_x']

[phases.install]
cmds = ['rm -f package-lock.json', 'npm install --legacy-peer-deps']

[phases.build]
cmds = ['npm run build']

[start]
cmd = './node_modules/.bin/serve -s build -l $PORT'
